<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <android.support.design.widget.AppBarLayout
        android:id="@+id/app_bar_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:theme="@style/AppTheme.AppBarOverlay">

        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="?attr/colorPrimary"
            android:theme="@style/AppTheme.ActionBar"
            app:popupTheme="@style/AppTheme.PopupOverlay">

            <TextView
                android:id="@+id/textView_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:ellipsize="end"
                android:maxLines="1"
                android:drawablePadding="10pt"
                android:textColor="@color/global_title_text_51"
                android:textSize="18pt"/>

        </android.support.v7.widget.Toolbar>
        <View
            android:id="@+id/title_devider_line"
            android:layout_width="match_parent"
            android:layout_height="1pt"
            android:layout_gravity="bottom"
            android:background="@color/title_devider_line"/>
    </android.support.design.widget.AppBarLayout>

    <ImageView
        android:id="@+id/toolbar_image_right"
        android:layout_width="30pt"
        android:layout_height="30pt"
        android:layout_gravity="center_vertical|right"
        android:layout_marginRight="15.3pt"
        android:elevation="5pt"/>
</FrameLayout>

