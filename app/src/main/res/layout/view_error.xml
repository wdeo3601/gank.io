<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/view_error"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

  <LinearLayout
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_gravity="center"
      android:gravity="center"
      android:orientation="vertical"
      >

    <ImageView
        android:layout_width="139pt"
        android:layout_height="139pt"
        android:src="@drawable/no_network_found"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20pt"
        android:text="当前无网络连接"
        android:textColor="@color/protocol_grey_color_153"
        android:textSize="17pt"
        />

    <Button
        android:id="@+id/view_error_retry"
        android:layout_width="100pt"
        android:layout_height="35pt"
        android:layout_marginTop="42pt"
        android:background="@drawable/shape_red_bt_bg"
        android:text="重新加载"
        android:textColor="@color/white"
        android:textSize="16pt"
        />
  </LinearLayout>
</FrameLayout>